
open mysql and have .env in springboot


to run backend
Open a terminal:

```powershell
cd springboot-Projectg
.\mvnw.cmd spring-boot:run

running http://localhost:8080


Front end
cd frontend-nextjs
npm install
npm run dev

running http://localhost:3000